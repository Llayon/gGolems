{
  "rules": {
    "lobbies": {
      ".read": true,
      "$roomId": {
        ".write": true,
        ".validate": "newData.hasChildren(['shortCode', 'hostPeerId', 'status', 'createdAt', 'updatedAt', 'expiresAt'])"
      }
    }
  }
}
